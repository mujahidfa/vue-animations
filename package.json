{
  "name": "vue-animations",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@vueuse/core": "^9.9.0",
    "motion": "^10.15.3"
  },
  "devDependencies": {
    "nuxt": "3.0.0"
  }
}
